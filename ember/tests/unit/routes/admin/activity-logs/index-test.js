import { moduleFor, test } from 'ember-qunit';

moduleFor('route:admin/activity-logs/index', 'Unit | Route | admin/activity logs/index', {

});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});